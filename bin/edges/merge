#!/usr/bin/env bash
# merge — an edge. Deterministic, no model, exit code selects the outgoing branch.
# Stub until cp-edge-scripts-zow; the contract it exits by is already real.
. "$(dirname "$(readlink -f "$0")")/_lib.sh"
edge_ok "edge merge: token=${CP_TOKEN:-?} factory=${CP_FACTORY:-?}"
