#!/usr/bin/env bash
# commit — an edge. Deterministic, no model, exit code selects the outgoing branch.
# Stub while the runner is being proven end to end; the real one lands with
# cp-edge-contract-vm6.
echo "edge commit: token=${CP_TOKEN:-?} factory=${CP_FACTORY:-?}"
exit 0
